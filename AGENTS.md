# AGENTS.md

**The instructions for this repository are in [`CLAUDE.md`](./CLAUDE.md). Read that file — all of
it — before working here.** It is short on purpose, and it ends with a "Where everything is" table
pointing at the rest: `STATUS.md` for what is built, `.claude/rules/` for the mandatory rules in each
area, and `docs/` for the reasoning behind them.

**Do not copy any of that content into this file.** This file was a full duplicate of `CLAUDE.md`
until 31 July 2026, kept in sync by hand. The sync failed: the copy drifted a whole slice behind on
what was built, still described the referral form as server-defined (`GET /public/referral-form`, an
endpoint that does not exist), and still linked a spec file that had been moved to the server repo.
A duplicate that is wrong is worse than no duplicate at all, because it reads exactly like the
truth. One file, one place to update it.
